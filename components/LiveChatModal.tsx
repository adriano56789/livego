import React from 'react';

// This component is currently a placeholder.
// The chat functionality has been moved directly into the LiveStreamViewerScreen
// to keep it docked at the bottom, as per user feedback.
// This component can be used in the future if a different chat modal behavior is desired.

const LiveChatModal: React.FC = () => {
  return null; 
};

export default LiveChatModal;
