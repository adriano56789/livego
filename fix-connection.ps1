$pathServer = "C:\Users\adria\OneDrive\Documentos\Área de Trabalho\livego\backend\src\server.ts"; $pathApi = "C:\Users\adria\OneDrive\Documentos\Área de Trabalho\livego\services\api.ts"; if (Test-Path $pathServer) { $content = Get-Content $pathServer -Raw; $content = $content -replace "origin: \[.*\]", "origin: \"*\""; $content | Out-File -FilePath $pathServer -Encoding utf8; Write-Host "? Server.ts corrigido (CORS liberado)" }; if (Test-Path $pathApi) { $content = Get-Content $pathApi -Raw; $content = $content -replace "return .http://localhost:3000.; // Usar localhost em desenvolvimento", "return window.location.protocol + \"//\" + window.location.hostname + \":3000\";"; $content | Out-File -FilePath $pathApi -Encoding utf8; Write-Host "? Api.ts corrigido (URL Base dinâmica)" }
