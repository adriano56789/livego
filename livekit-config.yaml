# ============================================
# LiveKit Server - Self-Hosted Configuration
# LiveGo - Produção VPS (72.60.249.175)
# ============================================

# Porta principal WebSocket (clientes conectam aqui)
port: 7880

# API Keys para autenticação de tokens
keys:
  LGK_d8d1c9c7ea47c4b754717542217a0d57: VXqZemGMtMohfwhhW7ZlVO-ss-CWTkBqr3nsAtUGcFEqlZ1-zQH7OUkd3_x3GLyJ

# Configuração WebRTC (STUN/TURN embutido)
rtc:
  # Range de portas UDP para media WebRTC
  port_range_start: 50000
  port_range_end: 60000
  # IP externo da VPS — OBRIGATÓRIO para funcionar fora de localhost
  node_ip: 72.60.249.175
  # Usar IP externo automaticamente
  use_external_ip: true
  # STUN servers para NAT traversal
  stun_servers:
    - stun:stun.l.google.com:19302
    - stun:stun1.l.google.com:19302
  # Codecs suportados
  enable_loopback_candidate: false

# TURN Server embutido no LiveKit (substitui coturn separado)
turn:
  enabled: true
  domain: livego.store
  tls_port: 5349
  udp_port: 3478

# Configuração de rooms
room:
  # Tempo máximo sem participantes antes de destruir a room (segundos)
  empty_timeout: 300
  max_participants: 500

# Logging
logging:
  level: info
  pion_level: warn
